# OS
.DS_Store
Thumbs.db

# Node (dev tooling only — the experiment itself has no build step)
node_modules/
npm-debug.log*
# transient tarball from `npm pack` (used by tests/bundler/run.mjs)
*.tgz

# IDE
.vscode/
.idea/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# Local Claude Code working notes — intentionally untracked (LaTeX aux is ignored in paper/.gitignore).
CLAUDE.md
